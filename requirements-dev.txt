# Dependencias solo de desarrollo/CI. La imagen Docker instala únicamente
# requirements.txt, así que nada de esto llega a producción.
-r requirements.txt

pytest>=8.3.0
pytest-cov>=6.0.0
ruff>=0.16.4
# Audita requirements.txt contra la base de vulnerabilidades de PyPI. Se ejecuta
# en su propio job de CI, informativo: un aviso nuevo no debe bloquear un PR que
# no lo introdujo, pero sí tiene que verse.
pip-audit>=2.7.3
