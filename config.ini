[server]
; Puerto HTTP del proyecto. Esta es la fuente de verdad: cámbialo solo aquí.
; Si se borra este valor, la app usará 5000 por defecto.
; Con Docker: usa ./docker-up.sh para lanzar el stack, que lee este valor y lo
; exporta como PORT antes de "docker compose up", así el mapeo de puertos y el
; contenedor siempre coinciden con lo que hay aquí.
; Si defines PORT manualmente en el entorno (o en .env sin usar docker-up.sh),
; ese valor tiene prioridad sobre este, por si necesitas un override puntual.
port = 5000
host = 0.0.0.0
; Activa el modo debug si es necesario.
debug = false
