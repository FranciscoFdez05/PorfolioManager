[server]
; Puerto HTTP del proyecto. Si se borra este valor, la app usará 5000 por defecto.
port = 5000
host = 0.0.0.0
; Activa el modo debug si es necesario.
debug = false
; Clave secreta opcional. Si no se indica, la app usará SECRET_KEY del entorno o una temporal.
secret_key =
