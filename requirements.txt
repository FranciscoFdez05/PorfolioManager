# Mínimos con parches de seguridad aplicados. El entorno tenía instalado
# Flask 3.0.3 / Werkzeug 3.0.3 / gunicorn 22.0.0, por debajo de lo que este
# fichero declaraba, así que lo desplegado no coincidía con lo documentado.
Flask>=3.1.0
# Werkzeug < 3.0.6: safe_join() no bloqueaba rutas absolutas en Windows
# (CVE-2024-49766) y el parseo multipart podía agotar recursos (CVE-2024-49767).
# safe_join es justo lo que usa send_from_directory para servir los estáticos.
Werkzeug>=3.1.8
python-dotenv>=1.0.1
# gunicorn < 23.0.0: request smuggling vía cabecera Transfer-Encoding (CVE-2024-6827)
gunicorn>=23.0.0
cryptography>=44.0.1
